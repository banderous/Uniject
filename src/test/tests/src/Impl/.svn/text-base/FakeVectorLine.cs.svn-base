using System;
using Testable;
using UnityEngine;

namespace Tests {
    public class FakeVectorLine : IVectorLine {
        public void Initialise(string name, Vector3[] points, Color colour, Material material, float width) {
            this.points3 = points;
        }

        public bool active { get; set; }
        public Vector3[] points3 { get; set; }
        public void draw() {
        }

        public void destroy() {
        }
    }
}

